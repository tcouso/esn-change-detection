from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A change detection algorithm based on echo state networks for remote sensing time series.',
    author='Tomás Couso Coddou',
    license='MIT',
)
